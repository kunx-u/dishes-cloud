server:
  port: 9003

spring:
  application:
    name: auth-service

  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: et2406
      config:
        namespace: et2406
        file-extension: yml
